plugins {
    id 'groovy-gradle-plugin'
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        'jlink-application' {
            id = 'ca.tjug.jlink-application'
            implementationClass = 'ca.tjug.gradle.plugins.jlink_application.JlinkApplicationPlugin'
        }
    }
}